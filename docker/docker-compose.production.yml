services:
  www:
    # Update this to the latest stable version before deploying.
    image: farmos/farmos:4.x.y
    volumes:
      - './sites:/opt/drupal/web/sites'
      - './keys:/opt/drupal/keys'
    ports:
      - '80:80'
    restart: always
